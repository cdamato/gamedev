# left hand side is the sprite's internal name.
# Parameters: filename, horizontal divisions, vertical divisions

bullet = {"bullet.png", 1, 1}
button = {"button.png", 3, 1}
highlight = {"highlight.png", 2, 1}
items = {"items.png", 2, 1}
player = {"player.png", 1, 1}
tilemap = {"tilemap.png", 5, 3}
checkbox = {"checkbox.png", 5, 1}
menu_background = {"menu_background.png", 2, 1}
slider = {"slider.png", 2, 1}
cursor_and_highlight = {"cursor_and_highlight.png", 2, 1}